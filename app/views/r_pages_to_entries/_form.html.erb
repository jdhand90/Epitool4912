<%= form_for(@r_pages_to_entry) do |f| %>
  <% if @r_pages_to_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@r_pages_to_entry.errors.count, "error") %> prohibited this r_pages_to_entry from being saved:</h2>

      <ul>
      <% @r_pages_to_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :r_page_id %><br>
    <%= f.number_field :r_page_id %>
  </div>
  <div class="field">
    <%= f.label :r_entry_id %><br>
    <%= f.number_field :r_entry_id %>
  </div>
  <div class="field">
    <%= f.label :properties %><br>
    <%= f.text_field :properties %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
