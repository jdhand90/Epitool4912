<%= form_for(@r_page) do |f| %>
  <% if @r_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@r_page.errors.count, "error") %> prohibited this r_page from being saved:</h2>

      <ul>
      <% @r_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :r_language_id %><br>
    <%= f.select :r_language_id, options_from_collection_for_select(RLanguage.all, 'id', 'language') %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :template %><br>
    <%= f.text_field :template %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="field">
    <%= f.label :properties %><br>
    <%= f.text_field :properties %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
