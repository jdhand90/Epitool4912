<%= form_for(@r_entry) do |f| %>
  <% if @r_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@r_entry.errors.count, "error") %> prohibited this r_entry from being saved:</h2>

      <ul>
      <% @r_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>  
  <div class="field">
    <%= f.label :r_language_id %><br>
    <%= f.select :r_language_id, options_from_collection_for_select(RLanguage.all, 'id', 'language') %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :entry %><br>
    <%= f.text_area :entry %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="field">
    <%= f.label :properties %><br>
    <%= f.text_field :properties %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
